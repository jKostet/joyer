<p id="notice"><%= notice %></p>

<h1>Listing Joys</h1>

<table>
  <thead>
    <tr>
      <th>Number</th>
      <th>Code</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @joys.each do |joy| %>
      <tr>
        <td><%= joy.number %></td>
        <td><%= joy.code %></td>
        <td><%= link_to 'Show', joy %></td>
        <td><%= link_to 'Edit', edit_joy_path(joy) %></td>
        <td><%= link_to 'Destroy', joy, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Joy', new_joy_path %>
